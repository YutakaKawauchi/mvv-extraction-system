# Frontend Environment Variables
# Copy this to .env.local for local development
# Copy this to .env.production for production build
# NEVER commit .env.local or real values to git!

# API Configuration
VITE_API_BASE_URL=http://localhost:8888/.netlify/functions
VITE_API_SECRET=your-development-secret

# Environment
VITE_ENVIRONMENT=development

# Authentication (for direct API access if needed)
# VITE_AUTH_USERNAME=admin
# VITE_AUTH_PASSWORD=dev_password_2025!